package main

import (
	"fmt"
)

// 6.15
// A + B = i+j
// ex: i = n-1, j = n-3
// P(i,j) is probability A wins by winning n games ahead of B
// P(i,j) = 1/2 * P(i+1, j) + 1/2 * P(i, j+1)

// P(n, j) = 1 where 0 <= j < n
// P(i, n) = 0 where 0 <= i < n



func main() {
	
}
